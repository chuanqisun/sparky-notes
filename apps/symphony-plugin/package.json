{
  "name": "@symphony/figma",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "scripts": {
    "dev": "npm run build -- --dev",
    "build": "rm -rf dist && node scripts/build.mjs"
  },
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.58.0",
    "@figma/widget-typings": "^1.5.0",
    "dotenv": "^16.0.3",
    "esbuild": "^0.17.8"
  },
  "dependencies": {
    "acs-expression-builder": "^0.0.2"
  }
}