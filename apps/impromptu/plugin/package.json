{
  "name": "@impromptu/plugin",
  "version": "1.0.0",
  "description": "Your Figma Plugin",
  "scripts": {
    "dev": "npm run build -- --dev",
    "build": "rm -rf dist && node scripts/build.mjs"
  },
  "license": "",
  "devDependencies": {
    "@figma/plugin-typings": "^1.72.0",
    "@figma/widget-typings": "^1.5.0",
    "dotenv": "^16.3.1",
    "esbuild": "^0.18.17"
  }
}